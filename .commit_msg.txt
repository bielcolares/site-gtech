feat: add PT/EN language switcher (i18n) across all pages

- Add LanguageContext (React Context + localStorage persistence)
- Add centralized translations.js with all PT/EN strings
- Update layout.js to wrap app with LanguageProvider
- Header: language toggle (flag + PT|EN) positioned after CTA button
- WhatsAppModal: fully bilingual (labels, placeholders, buttons)
- All components updated: Hero, RadarReciclagem, VideoSection,
  Features, Partners, ManifestoSection, AboutContent, ContactForm,
  Footer, ServicosContent (catalog grid + certifications modal)
